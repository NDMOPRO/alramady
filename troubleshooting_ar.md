# استكشاف الأخطاء

## المنصة لا تفتح
- تحقق من أن `rasid-gateway` و`rasid-frontend` يعملان.
- نفذ `docker ps`.
- إذا كان `gateway` متوقفًا فراجع تبعيات الخدمات الخلفية أولًا.

## تسجيل الدخول يفشل
- تحقق من أن `governance-service` يعمل.
- إذا كنت تعتمد على البيئة المحلية المزروعة فتأكد من تشغيل:
- `bash ./scripts/run-migrations.sh --full`

## الرفع في البيانات أو الصفحة الرئيسية لا يعمل
- تحقق من `data-service`.
- راجع اتصال PostgreSQL وMinIO.
- إذا كان الملف كبيرًا فتأكد من عدم تجاوز حدود الخدمة أو البوابة.

## المكتبة لا تعرض الأصول أو لا تفتح الملف
- تحقق من `library-service` و`minio`.
- تأكد من تمرير `x-tenant-id` و`x-user-id` عندما تختبر مباشرة على الـ API.
- فشل MinIO يظهر غالبًا كنجاح ناقص في بيانات الأصل أو كرابط تنزيل لا يعمل.

## التحليل لا يعرض نتائج
- تحقق من وجود `datasetId` صالح.
- راجع `dashboard-service`.
- إذا ظهرت البيانات لكن دون نتائج تحليلية فراجع سجلات المحرك داخل `dashboard-service`.

## التقارير لا تُصدَّر
- تحقق من `reporting-service`.
- إذا فشل PDF أو Word فراجع خدمات التصدير والقوالب التابعة للتقرير.
- أعد البناء أولًا ثم جرّب التصدير من جديد.

## العروض لا تتولد
- تحقق من `presentation-service` ومفتاح `OPENAI_API_KEY` إن كان المسار ذكياً.
- جرّب أولًا إنشاء عرض فارغ للتأكد من أن CRUD نفسه يعمل.

## مركز معرفة راصد لا يعمل
- تحقق من `ai-service`.
- راجع PostgreSQL وElasticsearch ومفتاح OpenAI.
- إذا فشل الإدخال أو الاستعلام فراجع سجلات `rag-engine`.

## القوالب الذكية لا تعيد استجابة
- تأكد من وجود قالب محفوظ ومحدد.
- راجع `POST /api/v1/ai/prompts/:id/test`.
- راقب استهلاك الرموز وسجلات الخطأ في `ai-service`.

## تحذير X-Forwarded-For في ai-service
- هذا التحذير مرتبط بإعداد `trust proxy`.
- هو تحذير معروف حاليًا ولا يمثل مانعًا مثبتًا لتشغيل الأسطح المعتمدة.

## المطابقة البصرية الصارمة غير مكتملة
- هذا ليس خطأ استخدام فقط.
- توجد قيود مثبتة في المسار الحالي لبعض حالات replication، لذلك يجب التعامل معها كقدرة جزئية لا كمكوّن مكتمل.

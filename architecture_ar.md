# معمارية المنصة

## الطبقات
- الواجهة الأمامية في `frontend` مبنية على Next.js.
- بوابة الدخول `gateway` مبنية على Nginx وتجمع كل المسارات تحت عنوان واحد.
- الخدمات الخلفية موزعة حسب المجال الوظيفي.
- التخزين يعتمد على PostgreSQL وRedis وElasticsearch وMinIO.

## الخدمات الأساسية للأسطح المعتمدة
- `data-service`: استيراد البيانات، قراءة المجموعات، الصفوف، الإحصاءات، والتصدير.
- `dashboard-service`: تحليل البيانات، لوحات المؤشرات، الثيمات، والمظهر.
- `reporting-service`: تعريف التقارير، الأقسام، البناء، التصدير، والجدولة.
- `presentation-service`: إنشاء العروض والشرائح والتوليد من النص أو المصدر والتصدير.
- `library-service`: حفظ الأصول والمجلدات والتنزيل والبث.
- `governance-service`: الهوية، التدقيق، الفرق، الأدوار، الأعلام الوظيفية، وسير العمل.
- `ai-service`: قواعد المعرفة، القوالب، الاختبارات الذكية، ومهام NLP وRAG.

## الخدمات المساندة
- `localization-service`: التعريب، اكتشاف اللغة، وتحويل المحتوى إلى RTL.
- `conversion-service`: التحويل بين الصيغ المدعومة.
- `replication-service`: التحليل البصري والمقارنة البصرية وإعادة البناء الجزئي.
- `template-service`: قدرات القوالب المساندة لبعض مسارات التصدير.
- `rendering-environment`: بيئة الرسم والتحقق البصري عالية الدقة.

## مسار التنفيذ النموذجي
- المستخدم ينفذ إجراءً من واجهة عربية.
- الواجهة تستدعي عميل API داخل `frontend/lib/api`.
- الطلب يمر عبر البوابة إلى الخدمة الخلفية المناسبة.
- الخدمة تنفذ منطق المجال وقد تستدعي خدمات أو مخازن إضافية.
- النتيجة تعود إلى الواجهة كمخرج فعلي أو ملف أو سجل جديد في قاعدة البيانات.

## أمثلة على الربط
- الصفحة الرئيسية تستدعي عدة خدمات بحسب نوع الملف والإجراء المختار.
- Surface التحليل ترسل `datasetId` إلى `dashboard-service` وتعرض نتائج التحليل المباشرة.
- Surface التقارير تنشئ تعريف التقرير ثم تضيف الأقسام ثم تبني المخرج عبر `reporting-service`.
- Surface المكتبة تحفظ الأصل في MinIO وتخزن بياناته الوصفية في الخدمة ثم تعيد استخدامه في البيانات أو العروض.
- Surface الإعدادات تستدعي الحوكمة للهوية والتدقيق وتستدعي `ai-service` لإدارة معرفة راصد وقوالبه.

## الثيمات والمظهر
- `dashboard-service` يحتوي على محرك الثيمات ومحرك المظهر.
- الثيمات تدعم أوضاع light وdark ومشتقات RTL وتطبيق Brand Kit.
- الواجهة تستهلك هذه القيم لتوحيد الشكل عبر الأسطح.

## القيود الحالية
- مسارات المطابقة البصرية موجودة، لكن المطابقة الحرفية واحد لواحد ليست محكومة على أنها مكتملة نهائيًا.
- بعض قدرات التدريب والنشر الإداري لا تزال محجوبة بسبب نقص schema أو runtime في المسارات الخاصة بها.

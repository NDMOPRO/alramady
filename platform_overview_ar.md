# نظرة عامة على منصة راصد

## ما هي راصد
- راصد منصة عربية أولًا لإدخال الملفات والبيانات وتحليلها وإنتاج تقارير وعروض ولوحات مؤشرات مع مكتبة لإعادة الاستخدام وسطح إعدادات تشغيلي.
- الواجهات المعتمدة في التشغيل الحالي هي: الصفحة الرئيسية، البيانات، التحليل، التقارير، العروض التقديمية، المكتبة، الإعدادات.
- التنفيذ الفعلي يمر عبر واجهة Next.js ثم بوابة Nginx ثم الخدمات الخلفية المتخصصة ثم قواعد التخزين والخدمات المساندة.

## الأسطح المتاحة الآن
- `/home`: نقطة الدخول الذكية. تدعم السحب والإفلات واختيار الملف وتعرض خيارات عربية ديناميكية بحسب نوع الملف والقدرات الفعلية.
- `/data`: استيراد البيانات، قراءة المجموعات، فتح التفاصيل، تصفح الصفوف، التصدير، والربط مع الموصلات السحابية المتاحة من خدمة البيانات.
- `/analysis`: تشغيل تحليل حي على `datasetId` واستخراج بروفايل البيانات ومؤشرات KPI وتوصيات الرسوم.
- `/reports`: إنشاء تقرير من مصدر فعلي ثم إضافة الأقسام وبناء التقرير وتصديره وجدولته.
- `/presentations`: إنشاء عرض فارغ أو توليد عرض من مصدر أو من موجز ذكي ثم متابعة التعديل والتصدير.
- `/library`: تخزين الأصول في المكتبة، تصفحها، تنزيلها، إعادة استخدامها، حفظ ثيمات ووصفات عمل وتشغيلها.
- `/settings`: إدارة المستخدمين والفرق والأعلام الوظيفية والتدقيق والمظهر ومركز إدارة راصد للمعرفة والقوالب.

## القدرات المدمجة داخل المنصة
- التعريب وتطبيق RTL من خلال خدمة التوطين.
- التحويل بين صيغ محددة مثل Word وPDF وMarkdown وHTML وExcel وCSV من خلال خدمة التحويل.
- التحليل البصري وإعادة بناء لوحة مؤشرات من صورة ومقارنة بصرية من خلال خدمة replication.
- مساعد راصد المضمن داخل الأسطح المعتمدة، ويتحدث بالعربية ويقترح إجراءات حقيقية مرتبطة بسياق الصفحة.
- إعادة استخدام الأصول والثيمات والوصفات من Surface المكتبة داخل الأسطح المرتبطة.

## البنية التشغيلية
- الواجهة الأمامية: `frontend` مبنية على Next.js 14.
- البوابة: `gateway` عبر Nginx.
- الخدمات الأساسية: `data-service`, `dashboard-service`, `reporting-service`, `presentation-service`, `library-service`, `governance-service`, `ai-service`.
- الخدمات المساندة: `localization-service`, `conversion-service`, `replication-service`, `template-service`, `excel-service`, `infographic-service`, `rendering-environment`.
- التخزين والبنية التحتية: PostgreSQL وRedis وElasticsearch وMinIO.

## ما هو مكتمل وما هو جزئي
- الأسطح السبعة المعتمدة تعمل ضمن المسار الفعلي الحالي وتظهر بالعربية مع اتجاه RTL.
- مركز إدارة راصد داخل الإعدادات يدعم قواعد المعرفة وقوالب السلوك وسير العمل واختبارها الحقيقي.
- توجد قدرات بصرية وتكرارية داخل المشروع، لكن المطابقة البصرية الحرفية واحد لواحد ما زالت تحتوي على حاجز تقني مثبت في مسارات replication لبعض المخرجات الثنائية، لذلك لا تُقدَّم هنا كقدرة مكتملة بالكامل.

## لمن هذه الوثيقة
- هذه الوثيقة مناسبة للقارئ الذي يريد فهم ما الذي تفعله المنصة حاليًا من دون تفاصيل تشغيلية عميقة.

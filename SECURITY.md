# سياسة الأمان — راصد RASID

## حساب مالك النظام (System Owner)
- **المستخدم:** MRUHAILY
- **البريد الإلكتروني:** prog.muhammed@gmail.com
- **الهاتف:** +966553445533
- **isOwner:** `true`
- **الدور:** `root_admin` — مالك النظام

## قواعد الحماية
1. حساب المستخدم الذي يحمل `isOwner: true` **لا يُحذف** ولا يُعطّل ولا يُخفّض دوره تحت أي ظرف.
2. علامة `isOwner` **لا تُمنح** لأي مستخدم آخر عبر API — فقط عبر `seed-owner` أو قاعدة البيانات مباشرة.
3. كل محاولة لتعديل أو حذف حساب المالك **تُسجَّل في سجل الأمان** (audit log) وتُرفض بخطأ `403`.
4. حساب المالك لا يمكن تغيير حالته إلى `INACTIVE` أو `SUSPENDED` أو `BANNED`.

## للمطورين
- **لا تُنشئ endpoints تتجاوز حمايات المالك** — أي route يعدّل/يحذف مستخدمًا يجب أن يمر عبر `protectOwner` middleware.
- **لا تُزِل `isOwner` check** من أي middleware أو service بدون موافقة مالك النظام.
- عند إضافة routes جديدة لإدارة المستخدمين، تأكد من تطبيق `protectOwner`.

## التواصل الأمني
- البريد: prog.muhammed@gmail.com
- الهاتف: +966553445533
